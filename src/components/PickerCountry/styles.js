import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    modalContainer: {
    },
    
    picker:{
        height: 50, width: 200, color:"#FFFFFF"
    }
})

export default styles;