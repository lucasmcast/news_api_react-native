import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    containerSearch:{
        marginTop: 100,
        justifyContent: "center",
        alignItems: "center"
    },
    inputSearch:{ height: 40, width: 300, borderColor: 'gray', borderWidth: 1 }
})

export default styles;